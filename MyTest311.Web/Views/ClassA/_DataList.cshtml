@using MetaShare.Common.Core.Presentation
@using MyTest311.Entities
@using MyTest311.Web.HtmlExtensions
@using MyTest311.Web.Models
<!--add customized code between this region-->
<!--add customized code between this region-->

@model IndexViewModel<ClassA>
        <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
		<th class="text-left">Name</th>
		<th class="text-center">Description2</th>
		<th class="text-left">AEType</th>
		<th class="text-left">ClassB</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            @if (Model.TargetPager != null && Model.TargetPager.Datas != null)
            {
            foreach (MyTest311.Entities.ClassA classAItem in Model.TargetPager.Datas)
                        {
                <tr>
			<td  class="text-left">
				@Html.DisplayFor(modelItem => classAItem.Name)
			</td>
			<td class="text-center">
				@Html.DisplayFor(modelItem => classAItem.Description2)
			</td>
			<td class="text-left">
				@Html.DisplayFor(modelItem => classAItem.AEType)
			</td>
			<td class="text-left">
				@Html.DisplayFor(modelItem => classAItem.ClassB.Name)
			</td>
                    <td>
                        @Html.ActionLink(" ", "Details", new { id = classAItem.Id, Controller = "ClassA", area = ""}, new {@class="m_details", title="Details" })
                        @Html.ActionLink(" ", "Edit", new { id = classAItem.Id, Controller = "ClassA", area = ""}, new {@class="m_edit", title="Edit" })
                        @Html.ActionLink(" ", "#", "#",null , new {href = "javascript:void(0);",@class="m_delete", title="Delete", onclick = " javascript:metaShare.basic.prototype.deleteTableColumn(this,'" + classAItem.Id + "','" + Url.Action("Delete",new {Controller = "ClassA", area = ""}) + "'); return false; "})
                    </td>
                </tr>
            }
            }
<!--add customized code between this region-->
<!--add customized code between this region-->
            </tbody>
        </table>


        <div class="UserPagerDiv text-center">
    @{
        object conditions = new
        {
            Name = Model.SearchModel != null ? Model.SearchModel.Name : "",
        };
    }
        @Html.AjaxRenderPager(Model.TargetPager, "Search", "ClassA","ClassA_DataList", conditions)
        </div>
